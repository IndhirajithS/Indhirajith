// src/testcase/App.test.js
import { render } from '@testing-library/react';
import App from '../App'; // Adjust this path if your App.js is somewhere else

test('renders the App component', () => {
  render(<App />);
});